import setuptools

setuptools.setup(
    name='erlyx',
    version='0.0.1',
    include_package_data=True,
    install_requires=['numpy'],
    python_requires='>=3.6',
    packages=['erlyx'],
    zip_safe=False)
